---
/**
 * 404 Not Found Page
 * 
 * Custom error page displayed when a requested page doesn't exist.
 * Features a minimal, centered layout with an icon, message, and
 * navigation back to the homepage.
 * 
 * Design:
 * - No header, navigation, or footer for focused experience
 * - Centered content with SVG icon
 * - Clear messaging about the error
 * - Single CTA button to return home
 * 
 * Route: /404 (automatically served for non-existent pages)
 * 
 * @page
 */

import { siteConfig } from '../config';

const pageTitle = '404 - Page Not Found';
---

<!DOCTYPE html>
<html lang={siteConfig.language}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    <meta name="robots" content="noindex, nofollow" />
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    
    <!-- Inline critical styles -->
    <style>
      :root {
        /* Colors - Dark theme */
        --color-bg: #0a0a0a;
        --color-text: #e5e5e5;
        --color-text-secondary: #a3a3a3;
        --color-text-muted: #737373;
        --color-accent: #3b82f6;
        --color-accent-hover: #2563eb;
      }

      [data-theme="light"] {
        --color-bg: #ffffff;
        --color-text: #171717;
        --color-text-secondary: #525252;
        --color-text-muted: #737373;
        --color-accent: #2563eb;
        --color-accent-hover: #1d4ed8;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        background: var(--color-bg);
        color: var(--color-text);
        line-height: 1.5;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem 1.5rem;
      }

      .container {
        max-width: 500px;
        width: 100%;
        text-align: center;
      }

      .icon {
        width: 80px;
        height: 80px;
        margin: 0 auto 2rem;
        color: var(--color-text-muted);
        opacity: 0.6;
      }

      .error-code {
        font-size: 1rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--color-text-muted);
        margin-bottom: 1rem;
      }

      h1 {
        font-size: 2rem;
        font-weight: 700;
        letter-spacing: -0.02em;
        margin-bottom: 1rem;
        color: var(--color-text);
      }

      .description {
        font-size: 1.125rem;
        line-height: 1.7;
        color: var(--color-text-secondary);
        margin-bottom: 2.5rem;
      }

      .home-button {
        display: inline-block;
        padding: 0.875rem 2rem;
        background: var(--color-text);
        color: var(--color-bg);
        font-weight: 600;
        font-size: 1rem;
        text-decoration: none;
        border-radius: 6px;
        transition: opacity 0.2s ease;
      }

      .home-button:hover {
        opacity: 0.85;
      }

      @media (max-width: 640px) {
        .icon {
          width: 64px;
          height: 64px;
          margin-bottom: 1.5rem;
        }

        .error-code {
          font-size: 0.875rem;
        }

        h1 {
          font-size: 1.5rem;
        }

        .description {
          font-size: 1rem;
          margin-bottom: 2rem;
        }

        .home-button {
          padding: 0.75rem 1.75rem;
          font-size: 0.9375rem;
        }
      }
    </style>

    <!-- Theme initialization script -->
    <script is:inline>
      (function() {
        const stored = localStorage.getItem('theme');
        const theme = stored || (window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark');
        document.documentElement.setAttribute('data-theme', theme);
      })();
    </script>
  </head>
  <body>
    <div class="container">
      <!-- 404 Icon -->
      <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="10"></circle>
        <line x1="12" y1="8" x2="12" y2="12"></line>
        <line x1="12" y1="16" x2="12.01" y2="16"></line>
      </svg>

      <div class="error-code">404 Error</div>
      
      <h1>Page Not Found</h1>
      
      <p class="description">
        The page you're looking for doesn't exist or has been moved. Let's get you back on track.
      </p>

      <a href="/" class="home-button">Back to Home</a>
    </div>
  </body>
</html>
